{
  "name": "esplanada-presentation",
  "private": true,
  "type": "module",
  "scripts": {
    "audio": "node generate-audio.mjs",
    "capture": "node capture.mjs",
    "capture:web": "node capture.mjs --only=web",
    "capture:pms": "node capture.mjs --only=pms",
    "render": "node render.mjs",
    "render:vo-only": "node render.mjs --vo-only",
    "qa": "node qa.mjs"
  },
  "comment": "Reuses playwright + chromium from the parent presentation/ (node resolves it up the tree). If it can't be found, run `npm i playwright && npx playwright install chromium` here.",
  "dependencies": {
    "playwright": "^1.49.0"
  }
}
